# Generated by offline/build_wheelhouse.sh from pyproject.toml.
# Edit pyproject.toml, not this file.

# core — ingest, graph, features, rules, fusion
maxminddb>=2.6
networkx>=3.3
numpy>=1.26
pandas>=2.2
pyarrow>=16.0
pydantic>=2.7
pyyaml>=6.0
scikit-learn>=1.5

# api — the console's backend and the PDF report
fastapi>=0.110
httpx>=0.27
joblib>=1.4
reportlab>=4.1
uvicorn>=0.29

# dev — the test suite, run as part of the offline checklist
pytest>=8.2
ruff>=0.5

# gnn — torch comes from the PyTorch CPU index, see the script
torch-geometric>=2.5
torch>=2.2

